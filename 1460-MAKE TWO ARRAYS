class Solution {
    public boolean canBeEqual(int[] target, int[] arr) {
        int[] u=new int[1001];
        int[] v=new int[1001];
        for(int h:target){
            u[h]++;
        }
        for(int h:arr){
            v[h]++;
        }
        return Arrays.equals(u,v);
    }
}
